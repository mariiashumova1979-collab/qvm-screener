# Зависимостей нет: функция использует только стандартную библиотеку Python
# (urllib, json, math, http.server). pandas/numpy/yfinance намеренно НЕ нужны —
# это держит бандл лёгким и холодный старт быстрым.
#
# Если позже добавишь что-то (например, requests вместо urllib), впиши сюда.
